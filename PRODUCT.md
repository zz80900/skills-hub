# Product

## Register

product

## Users

NEXGO Skills 面向三类任务型用户：需要查找、阅读并安装 Skill 的内部开发者；负责上传、升级和维护本地 Skill 的贡献者；负责用户、分组、权限与可见范围治理的管理员。用户通常在工作台或开发环境中使用它，目标是快速判断某个 Skill 是否可用、如何安装、谁维护、当前版本是否可信。

## Product Purpose

这是一个 Skills 库管理系统，负责管理本地 Skill 资产并聚合只读的 `skills.sh` 外部来源。公开侧提供统一搜索、来源切换、详情查看和安装命令复制；登录后的工作台提供 Skill 创建、ZIP 上传、升级、版本追溯、逻辑删除、分组和用户管理。成功标准是本地库可控、外部库可发现、上传链路可信、版本历史清楚，并且远程源失败不影响本地库使用。

## Brand Personality

温和、可信、精确。界面借鉴 `DESIGN-claude.md` 的暖画布和编辑气质，但默认服务任务流而非营销叙事：文案要短、确定、可执行；视觉要让用户感到系统可靠、边界清楚、操作后果可预期。

## Anti-references

不要把它做成通用 CMS、复杂 RBAC 平台或外部 Skill 镜像库。视觉上避免冷蓝灰 SaaS 模板、Spotify 式深色荧光界面、玻璃拟态、过度圆角、重复图标卡片网格、英雄区大数字指标模板，以及用弹窗承载本该内联解决的常规流程。

## Design Principles

1. 本地资产优先：本地库是可管理资产，外部源只是发现入口，界面必须持续区分来源、权限和可操作性。
2. 操作可信：创建、升级、删除、重置密码等动作必须明确展示范围、结果和失败原因，避免含糊确认。
3. 文档即交付物：Skill 描述、安装命令、版本历史和 ZIP 约束是核心内容，不是辅助说明。
4. 暖而克制：采用 Claude 风格的暖画布、珊瑚主动作和深色代码面板，但保持产品界面的密度、稳定和一致。
5. 不扩张边界：优先服务本地库管理、外部只读聚合和用户治理，不预留 CMS、复杂权限或内容富编辑能力。

## Accessibility & Inclusion

默认目标为 WCAG 2.2 AA。正文和占位文本对比度不低于 4.5:1，大字号不低于 3:1；所有可交互元素必须支持键盘访问、清晰焦点态和足够触控面积。动效仅表达状态变化，并提供 `prefers-reduced-motion` 降级。中文文案优先，错误信息需要说明用户下一步可做什么。
