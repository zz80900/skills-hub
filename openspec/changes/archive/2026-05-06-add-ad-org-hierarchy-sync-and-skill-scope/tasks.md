## 1. AD 组织架构同步基础

- [x] 1.1 扩展 `users` 数据模型与 schema 兼容升级逻辑，新增 AD DN 快照、组织层级、组织路径和层级深度字段
- [x] 1.2 在 AD 认证服务中实现 OU 路径解析与标准化，按“去掉共享顶级根 OU、最多保留 4 级、从高到低落库”的规则生成组织架构
- [x] 1.3 在 AD 用户首次创建与后续登录同步流程中写入并覆盖组织架构字段，保证每次成功登录都刷新最新组织信息
- [x] 1.4 扩展用户序列化与当前登录用户信息接口，返回个人中心展示所需的组织层级字段

## 2. Skill 组织范围模型与后端权限

- [x] 2.1 扩展 `skills` 数据模型与 schema 兼容升级逻辑，新增范围类型和组织范围字段，并把历史数据回填为 `PUBLIC` 或 `GROUP`
- [x] 2.2 调整 Skill 创建、更新相关 schema 与 service，支持 `PUBLIC`、`GROUP`、`ORGANIZATION` 三种范围类型及其参数校验
- [x] 2.3 实现组织范围候选项查询与普通用户“只能选择自己末级组织”、管理员“可选择任意已存在组织节点”的约束
- [x] 2.4 扩展首页本地 Skill 列表、本地 Skill 详情和版本详情的可见性判断，统一支持公开、组范围和组织范围的权限过滤
- [x] 2.5 保持 workspace 管理权限只基于 owner 和管理员角色，验证组织可见用户不会获得编辑、升级、删除权限

## 3. 前端个人中心与 Skill 表单

- [x] 3.1 调整用户中心数据读取与展示区域，在个人中心展示当前用户的组织架构层级并兼容少于 4 级的情况
- [x] 3.2 调整 Skill 创建/编辑表单，新增范围类型选择并按类型切换公开、归属组、归属组织的目标输入控件
- [x] 3.3 在 Skill 表单中接入组织候选项接口，正确处理普通用户末级组织限制和管理员全量组织选择
- [x] 3.4 在 Skill 列表、详情或管理视图中展示当前 Skill 的可见范围，帮助用户识别其归属组或归属组织

## 4. 测试与回归

- [x] 4.1 为 AD OU 解析与登录同步补充后端测试，覆盖 4 级组织、少于 4 级组织、共享根 OU 裁剪和登录覆盖更新场景
- [x] 4.2 为 Skill 组织范围校验与可见性补充后端测试，覆盖公开、组范围、组织范围、祖先部门、子部门、兄弟部门和无组织本地用户场景
- [x] 4.3 为前端个人中心与 Skill 表单补充交互验证，确认组织架构展示、范围选择和兼容现有组范围交互都正常
- [x] 4.4 手动回归 AD 用户登录、个人中心查看、Skill 创建/更新、首页检索和本地详情访问流程，确认旧有组范围能力未回归
